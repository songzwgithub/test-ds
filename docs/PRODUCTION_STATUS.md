# Production status

Version: **1.0.0**

The active pipeline contains 32 semantic production stages packaged inside the `pypsds` distribution. Historical experiment-step identifiers and legacy development trees are not part of the current branch; Git history is the provenance archive.

The sanitized reference regression is stored in `release/reference_case_regression.json`.
