# Release validation

Machine-specific inventory/output-contract snapshots are generated locally under `docs/release/generated/` and are not committed. The portable release keeps source-level documentation and a sanitized reference-case regression summary only.
