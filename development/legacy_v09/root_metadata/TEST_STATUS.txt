pyPSDS-GAMMA v0.9 standalone package validation

Static compile:
  python -m compileall pypsds scripts tests
  PASS

Unit tests:
  5 passed
  - Dolphin GLRT equal-scale sanity
  - Moraine KS basic sanity
  - candidate coherence kernel perfect-support sanity
  - robust phase-link + temporal-coherence perfect-phase sanity
  - full-scene example config sanity

Wheel build:
  pypsds_gamma-0.9.0-py3-none-any.whl
  PASS

Wheel isolated import:
  pypsds.__version__ == 0.9.0
  strict-upper-triangle pair count for 38 dates == 703
  PASS

Not executed in the artifact container:
  GAMMA SLC2pt/data2pt/phase_sim_orb_pt runtime, because the user's GAMMA installation
  and RSLC data are not mounted here. The corresponding provider/code path is carried
  forward from the validated prototype and the v0.9 phase-cache builder fixes the
  tile-global-origin variable shadowing issue from the ad-hoc 07c script.
