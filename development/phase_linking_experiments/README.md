# Historical Phase-Linking Experiments

This directory contains pre-release scientific audit, parity, benchmark, and
prototype utilities retained only for provenance.

Files carrying historical `u33`, `u34`, or `u35` experiment identifiers are
not production commands and are not part of the active pipeline. Production
execution is defined by `pypsds.pipeline.STAGES` and the semantic scripts in
`scripts/`.
